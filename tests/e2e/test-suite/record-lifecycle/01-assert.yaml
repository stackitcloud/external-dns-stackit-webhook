apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
commands:
        - command: ../../scripts/check-dns.sh A "e2e-a.${ZONE_NAME}" "" "${ZONE_NAME}"
        - command: ../../scripts/check-dns.sh AAAA "e2e-aaaa.${ZONE_NAME}" "" "${ZONE_NAME}"
        - command: ../../scripts/check-dns.sh CNAME "e2e-cname.${ZONE_NAME}" "" "${ZONE_NAME}"